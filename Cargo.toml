[package]
name = "pica"
version = "0.1.0"
authors = ["Nico Wagner <n.wagner@dnb.de>"]
edition = "2018"
license = "AGPL-3.0-only"

[dependencies]

[lib]
path = "src/lib.rs"
name = "pica"

[profile.release]
codegen-units = 1
panic = "abort"
lto = true

